package com.github.se.wanderpals.model.data

/**
 * Enum representing different roles a user can have within the application.
 * Each role has different levels of access and permissions.
 */
enum class Role {
    OWNER, ADMIN, MEMBER, VIEWER
}